# Set Examine
- type: entity
  id: ActionSetExtraExamine
  name: set-examine-title
  description: set-examine-dialog
  categories: [ HideSpawnMenu ]
  components:
  - type: InstantAction
    itemIconStyle: NoItem
    useDelay: 3
    event: !type:SetExamineActionEvent

# TODO
# The action needs an icon, but what?
# This is technically outdated from the set-pose PR, so I should make sure everything is up to date.
# Make sure its still predicted
